# Public base URL of the deployed site (used by Next.js for SEO / links).
# Not required for local dev.
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# MongoDB (reserved for Phase 2 features like orders storage).
# Not used in Phase 1 — leave blank if you're not using it yet.
MONGO_URL=
